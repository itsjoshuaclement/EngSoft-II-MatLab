function [output1,output2] = sumAndAverage(input)
% takes in an array of numbers and returns the sum and average of the array
%   takes the sum of input, and takes the mean of input. And outputs sum as
%   output1 and mean as output2
output1 = sum(input);
output2 = mean(input);
end

